# Original photos — 182 MB of HEIC and video. Kept locally, not in the repo:
# too large and slow for git, and only the converted images are needed to build
# the site. Back these up separately (external drive / cloud).
source-photos/

# Build output — regenerated by "UPDATE SITE.bat" whenever it's needed
aaina-space-upload.zip

# Scratch
manifest.json
*.pyc
__pycache__/
.DS_Store
Thumbs.db
